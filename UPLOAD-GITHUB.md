# Upload to GitHub / Netlify

Replace every file in the repository root and the complete `icons` directory. The new `rail-services.js` file is required.

Recommended commit:

`Release Galizische Bahn v0.9.1`

After GitHub finishes the commit, run **Clear cache and deploy site** in Netlify. Remove an installed older PWA once if Safari still displays a previous service-worker cache.
